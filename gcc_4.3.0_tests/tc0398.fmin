int printf (const char *, ...);
int a, c, d = 6, e;
int *b, *f = &e, *g;
int
main ()
{
    int **h = &b;
    *f = d > (h == &b);
    for (; c >= 0; c--)
    {
        if (*f)
            continue;
        g = &a;
        f = g;
    }
    *f = 1;
    printf ("%d\n", a);
    return 0;
}
