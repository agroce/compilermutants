int printf (const char *, ...);
int a, b, c, d;
int
main ()
{
    {
        d = (a ^ 1) < 0 ? a : 1;
    }
    c = 0;
    for (; c != 13; ++c)
    {
        {
            int *e = &b;
            d = (a ^ 1) < 0 ? 0 : a;
            *e = d;
        }
    }
    printf ("%d\n", b);
    return 0;
}
