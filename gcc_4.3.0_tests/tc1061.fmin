int printf (const char *, ...);
int a;
int **b;
int *c = &a;
void
fn1 (p1)
{
    int ***d = &b;
    if ((&b == d) < p1 > (0 & 1UL))
        *c = 1;
}

int
main ()
{
    fn1 (2UL);
    printf ("%d\n", a);
    return 0;
}
