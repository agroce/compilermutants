int printf (const char *, ...);
int a, b, c, d, e, f = 1, g, h, i, k, n, o, p, q, r, s, t, u, v, w, y, z, t1;
short j;
int **l, **m;
int *x;
static int
fn1 (short *p1, char p2)
{
    if (((k = 0) == 1) < (((l = &x) == (m = &x)) < p2))
    {
        for (; r; r++)
            p = 0;
        t1 = 1 + d;
        if (t1)
            n = 0;
        z = e ? 0 : 0 / 0;
        if (z)
            o = 0;
        g = 0;
        g = 0;
        f &= *p1;
        y = 1 & -c ^ c < 0 ? 0 : -c;
        u = 0;
        w = 1 + d;
        v = 0;
        v = e ? 0 : 0 / 0;
        w = 0;
        u = a && b > 0 > -b || a && b && 0 < -b ? 0 : 1;
        y = 0;
        q = 0;
        r = 0;
        k = p2 | t || (i = p2 == 0 != h & s);
    }
    return 0;
}

int
main ()
{
    fn1 (&j, 154L);
    printf ("%d\n", f);
    return 0;
}
