int printf (const char *, ...);
int a, e;
short b;
unsigned char ****c;
short *d = &b;
unsigned char ***f;
int
fn1 (p1)
{
    return p1 && a > 0 > -a || p1 && a && 0 < -a ? 0 : p1 + 1;
}

int
main ()
{
    e = fn1 (&f == (c = &f));
    *d = e;
    printf ("%d\n", b);
    return 0;
}
