int printf (const char *, ...);
char a;
int b, c, d, e;
void
fn1 (unsigned char p1)
{
    for (;; e++)
        break;
    if ((d = a = p1) >= p1);
    else
        for (; b <= 0; b++);
}

int
main ()
{
    fn1 (c | 143L);
    printf ("%d\n", b);
    return 0;
}
