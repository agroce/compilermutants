int printf (const char *, ...);
int a = 1, b = 1, c, d;
int *e = &a;
short f;
int
main ()
{
    short *g;
    if (!b);
    else
    {
        c = 0;
        for (; d <= 1; d++);
    }
    if (((g = &f) == &f) >= d)
        *e = 0;
    printf ("%d\n", a);
    return 0;
}
