int printf (const char *, ...);
int a = 1;
char
fn1 (char p1, char p2)
{
    return p1 && p2 / p1 ? 0 : p1 * p2;
}

int
main ()
{
    int b;
    b = 2;
    for (; b >= 0; b--)
        if (fn1 (147L, b));
        else
            a = 0;
    printf ("%d\n", a);
    return 0;
}
