int printf (const char *, ...);
int a, b, c, d, e, f, g, h, j, k, l, m;
static int i = 0;
int
fn1 (p1)
{
    return c == 0 ? p1 : 0 % 0;
}

int
fn2 (char p1)
{
    f = 0;
    for (; f >= 0; f--)
    {
        int n;
        l = e == 0 ? 0 : 0 % 0;
        k = f & 1 || (g = p1);
        m = d == 0 ? 0 : 0 / 0;
        h = n = fn1 (0 < m);
        if (h);
        else
        {
            for (; i;)
                return n;
            b && a && 0 / 0 || b && a && b && 0 / 0 * a;
            h = 0;
            return g;
        }
    }
    return 0;
}

int
main ()
{
    if (fn2 (1))
        j = 1;
    printf ("%d\n", j);
    return 0;
}
