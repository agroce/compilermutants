int printf (const char *, ...);
int a, d;
char b = 1;
int *c = &a, *g = &a;
char *e = &b;
unsigned char **f;
void
fn1 (unsigned short *p1)
{
    unsigned char ***h = &f;
    *g = 0 < (short) (h == &f | *p1);
}

int
main ()
{
    unsigned short i = 35459L;
    fn1 (&i);
    d = *c;
    if (d);
    else
        *e = 0;
    printf ("%d\n", b);
    return 0;
}
