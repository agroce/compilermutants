int printf (const char *, ...);
int a, d;
int **b;
int *c = &a;
int
fn1 (int p1, int p2)
{
    return p1 * p2;
}

int
main ()
{
    d = fn1 (&c == (b = &c), 5);
    **b = d;
    printf ("%d\n", a);
    return 0;
}
