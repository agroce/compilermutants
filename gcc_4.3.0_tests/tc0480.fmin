int printf (const char *, ...);
int a, b, d, e;
int *c = &b;
int
fn1 (int p1, int p2)
{
    for (; p1;)
        e = a ? 0 : 0 % 0;
    return p2;
}

int
main ()
{
    *c = fn1 (0, &d == (0, &d));
    printf ("%d\n", b);
    return 0;
}
