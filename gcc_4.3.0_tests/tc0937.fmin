int printf (const char *, ...);
int a = 1, c = 1, e, f, g, h, i, j;
static int b[] = { 0 };

int d[3];
int
fn1 (int p1, int p2)
{
    return p2 == 0 ? p1 : 0;
}

int
main ()
{
    for (; e < 1; e++)
        d[2] = 1;
    f = 2;
    for (; f; f--)
    {
        i = fn1 (a, d[f] && 0 <= 0);
        h = 1 ? b[0] : 0;
        int k = i;
        j = h > k || h <= 0 && k <= 0 && k ? 0 : 1;
        if (d[f])
            g = a;
        if (g)
            c = 0;
    }
    printf ("%d\n", c);
    return 0;
}
