int printf (const char *, ...);
int a, b, c;
int
fn1 (char p1, char p2)
{
    return p1 > 0 && p1 > 127 - p2 || p2 < 0;
}

int
main ()
{
    int d = 878L;
    b = fn1 (a || c < 1, d);
    printf ("%d\n", b);
    return 0;
}
