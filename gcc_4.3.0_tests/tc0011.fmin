int printf (const char *, ...);
union U1
{
    int f1;
    int f2;
}
a, d;
union
{
    int f0;
    int f2;
}
c;
int b;
int
fn1 (int p1, int p2)
{
    return p1 * p2;
}

void
fn2 (int *p1)
{
    int e;
    d.f1 = 2;
    union U1 *f[] = { &a };
    e = -1;
    b = fn1 (&a == f[0], e);
    *p1 &= d.f2;
    for (; c.f0 <= 0; c.f0++)
        if (b)
            break;
}

int
main ()
{
    fn2 (&b);
    printf ("%d\n", c.f2);
    return 0;
}
