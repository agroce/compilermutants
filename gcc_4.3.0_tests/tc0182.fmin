int printf (const char *, ...);
int a, b, c, g;
char d;
char *e = &d;
int *f;
int
fn1 (p1)
{
    return a && p1 && 0 > p1 || a && p1 < 0 && p1;
}

int
fn2 (p1)
{
    return p1 - 1;
}

void
fn3 (int **p1)
{
    fn1 (0);
    b = 0;
}

int
main ()
{
    int *h = &c;
    fn3 (&h);
    f = *&h;
    g = fn2 (h != f);
    *e = g;
    printf ("%d\n", d);
    return 0;
}
