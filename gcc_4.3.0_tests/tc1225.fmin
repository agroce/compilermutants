int printf (const char *, ...);
int a, b = 1, c, d, f;
const int e[][4][1] = { {}, 0, 0, 0, 1 };

unsigned g;
static int
fn1 ()
{
    for (;;)
    {
        if (e[1][3][0] | g)
            b = c;
        for (; f; f++);
        for (;;)
        {
            if (a)
                break;
            d++;
            for (; d;)
                return 0;
            for (;;);
        }
    }
}

int
main ()
{
    fn1 ();
    printf ("%d\n", c);
    return 0;
}
