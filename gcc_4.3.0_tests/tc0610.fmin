int printf (const char *, ...);
int a, b;
int c[1];
int *d = &b;
int
fn1 (p1)
{
    return p1 && a > 0 > -a || p1 && a && 0 < -a ? 0 : p1 + 1;
}

int
main ()
{
    *d = fn1 (&c[0] != &c[1]);
    printf ("%d\n", b);
    return 0;
}
