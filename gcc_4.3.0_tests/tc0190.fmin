int printf (const char *, ...);
int a, b;
long long *c;
int *d = &a;
void
fn1 (p1)
{
    if (p1)
        for (; b >= 0; b--);
}

int
main ()
{
    long long **e = &c;
    for (; a < 22; a = a + 1)
        fn1 ((&c == e) >= *d ^ 1);
    printf ("%d\n", b);
    return 0;
}
