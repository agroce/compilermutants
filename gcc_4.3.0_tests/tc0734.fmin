int printf (const char *, ...);
int a;
unsigned short b;
int *c = &a;
void
fn1 (p1)
{
    unsigned short *d;
    unsigned short **e = &d;
    *c = p1 <= ((*e = &b) != &b);
}

int
main ()
{
    int f = -1L;
    fn1 (f);
    printf ("%d\n", a);
    return 0;
}
