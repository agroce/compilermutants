int printf (const char *, ...);
int a, b = 1, c, d;
char
fn1 (char p1, char p2)
{
    return p1 > 0 && p2 && p1 > p2 || p2 < 0 ? p1 : 1 + p2;
}

int
main ()
{
    int e = 2087L;
    c = fn1 ((0, 0) ^ 1 && b, e);
    d = 0 ? 0 : 1 % c;
    if (d)
        a++;
    printf ("%d\n", a);
    return 0;
}
