int printf (const char *, ...);
int a, b, c, d, e, i, l, o, q, r, s, t;
struct
{
    short f4;
    int f5;
}
h, k;
char f;
char *g = &f, *x;
short j;
char **m;
short *n = &j;
int *p;
char ***w = &m;
int
fn1 (p1)
{
    return e == 0 ? p1 : 0 % 0;
}

static short fn2 ();
void fn3 ();
void
fn4 ()
{
    fn3 ();
    fn2 ();
}

short
fn2 ()
{
    int *u = &k.f5, *v = &h.f5;
    *u = 4LL | 0 != s;
    t = a && 0 / 0 && 0 / 0 || a && b && a && 0 / 0 * b;
    o = *n = 0;
    if (0, (p = 0) == 0 && *u)
    {
        for (; i;)
            *v = l > 0;
        q = c && d && d || c && d < 0 && 1 - d ? 0 : 1 + d;
    }
    else
        u = v;
    r = fn1 (*u);
    **m = r;
    return 0;
}

void
fn3 ()
{
    x = g;
    *w = &x;
}

int
main ()
{
    fn4 ();
    printf ("%d\n", f);
    return 0;
}
