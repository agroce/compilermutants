int printf (const char *, ...);
int a, b, c, d, e;
int *f = &c;
int
fn1 (p1)
{
    return p1 && a && 0 > -a || p1 && a < 0 < -a ? 0 : p1 + 1;
}

void
fn2 (p1)
{
    *f = p1;
}

int
main ()
{
    int *g = &b, *h = &b;
    e = fn1 ((h != g) < 1);
    d = 1 ? e : 0;
    *g = d;
    fn2 (*g);
    printf ("%d\n", c);
    return 0;
}
