int printf (const char *, ...);
int a, f;
long long **b;
static long long ***c = &b;
char d;
char *e = &d;
int
fn1 (p1)
{
    for (; a; a++)
        return 0;
    return p1;
}

void
fn2 (p1)
{
    *e = p1;
}

int
main ()
{
    long long ***g = &b;
    f = fn1 (5L);
    fn2 (f <= (c == g));
    printf ("%d\n", d);
    return 0;
}
