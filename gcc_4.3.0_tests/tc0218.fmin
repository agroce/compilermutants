int printf (const char *, ...);
int a = 5, b, c;
void
fn1 (p1)
{
    if (1 ^ p1)
        for (; c != 1; --c);
}

int
main ()
{
    int *d = &b;
    fn1 ((&b == d) < a);
    printf ("%d\n", c);
    return 0;
}
