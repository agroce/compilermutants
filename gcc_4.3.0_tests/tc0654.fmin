int printf (const char *, ...);
int a, b, c, d = 1, e, f, g, h, k, l, m, n;
unsigned i;
int j[6];
void
fn1 (p1)
{
    int o;
    h = 0;
    for (; h <= 1; h++)
    {
        e = 0;
        for (; e <= 0; e++)
            if (d)
            {
                i = 0;
                for (; i <= 0; i++)
                {
                    m = 0 == p1;
                    o = 1 ^ m ^ 1 & c ^ c < 0 ? m : m - 1;
                    j[i + 1] = o;
                }
            }
            else
                for (; k; k++)
                {
                    g = 0;
                    for (; g <= 0; g++)
                        j[0] = 0;
                }
    }
    for (; f; f++)
        n = a && 0 / 0 / 0 || a && b && a && 0 / 0 * b;
    h = 0 != l != i ^ n;
}

int
main ()
{
    fn1 (0);
    printf ("%d\n", j[1]);
    return 0;
}
