int printf (const char *, ...);
unsigned short *a;
long long b;
long long *c = &b;
int d, e;
int
main ()
{
    unsigned short **f = &a, **g = &a;
    for (; d != 7; ++d);
    e = (g == f) >= d;
    *c = e;
    printf ("%d\n", (int) b);
    return 0;
}
