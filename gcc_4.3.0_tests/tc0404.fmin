int printf (const char *, ...);
int a, c, d, f, g, h, j, k, l, m, n, p, q, r;
char b;
struct
{
    int f2;
    long f3;
}
e, o =
{
    0, 1
}

, *i = &e;
void
main ()
{
    *i = o;
    *i = *i;
    printf (&b, 0);
    a += f;
    printf (&b, 0);
    a += g;
    printf (&b, 0);
    a += h;
    int s = e.f3;
    printf ("%s %d\n", &b, s);
    a += 0;
    for (; q; q++)
        if (r)
            printf (&b, k);
    a += k;
    if (r)
        printf (&b, j);
    a += j;
    if (r)
        printf (&b, m);
    a += m;
    if (r)
        printf (&b, 0);
    a += l;
    if (r)
        printf (&b, 0);
    a += 0;
    if (r)
        printf (&b, n);
    a += n;
    if (r)
        printf (&b, 0);
    a += 0;
    if (r)
        printf (&b, 0);
    a += 0;
    if (r)
        printf (&b, p);
    a += p;
    if (r)
        printf (&b, 0);
    a += 0;
    if (r)
        printf (&b, 0);
    a += 0;
    if (r)
        printf (&b, 0);
    a += 0;
    if (c)
        printf ("%x\n", d);
}
