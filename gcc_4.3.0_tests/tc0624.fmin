int printf (const char *, ...);
int a, b, c = 1, d, e, g, h, i = 953L, k;
char f, j;
char
fn1 ()
{
    short l;
    for (;; f++)
    {
        k = b >= 0 || a > 1 >> b ? a : a < b;
        g = f;
        e = j;
        for (; d != 1; d++)
        {
            short m = j;
            int n = j;
            l = m || n >= 1 ? m : m << n;
            if (l)
                c = 0;
            h = 0 >= f;
            if (h)
                return j;
            i = 0;
        }
    }
}

int
main ()
{
    fn1 ();
    fn1 ();
    printf ("%d\n", c);
    return 0;
}
