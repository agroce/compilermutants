int printf (const char *, ...);
int *a;
int b = 91, c;
void
fn1 (p1)
{
    if (p1);
    else
        for (; c <= 0; c++);
}

int
main ()
{
    int **d = &a, **e = &a;
    fn1 ((e == d) < b);
    printf ("%d\n", c);
    return 0;
}
