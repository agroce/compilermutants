int printf (const char *, ...);
const unsigned ****a;
static const unsigned *****b = &a;
int c, d, e;
int
fn1 (int p1, int p2)
{
    return p1 + p2;
}

int
fn2 ()
{
    for (; c <= 0; c++)
    {
        e = fn1 (b == &a, 6L);
        d = 1 % e;
        if (d)
            return 0;
    }
    return 0;
}

int
main ()
{
    fn2 ();
    printf ("%d\n", c);
    return 0;
}
