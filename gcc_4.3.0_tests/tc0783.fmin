int printf (const char *, ...);
char a;
int b, c;
int *d = &c, *f = &b;
const unsigned char e = 250;
int
main ()
{
    for (;; b++)
    {
        if ((a <= e) - *f);
        else
            break;
        *d = 1;
    }
    printf ("%d\n", c);
    return 0;
}
