int printf (const char *, ...);
int a, b, c, d, f, g, h, i, j, k, l, m;
short e;
int
fn1 (int p1, int p2)
{
    return 0 / p1 && 0 / 0 && p2 && p1 && 0 / 0 ? 0 : p1 * p2;
}

void
fn2 ()
{
    short n;
    int o[4];
    h = 0;
    for (; h < 4; h++)
        o[h] = 1;
    n = e < 0 ? e : e > 1;
    g = o[0] ^ n < 0 > o[0];
    l = a && 0 > -b ? 0 : a + 1;
    for (; f < 1; f = 1)
        k = 0 ? 0 : e << 1;
    m = fn1 (k || i, 1);
    l = 1 & c ^ c < 0 ? 0 : m - 1;
    j = d == 0 ? l : 0 % 0;
}

int
main ()
{
    e = 1;
    fn2 ();
    printf ("%d\n", j);
    return 0;
}
