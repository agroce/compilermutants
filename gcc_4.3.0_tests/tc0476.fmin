int printf (const char *, ...);
int a = 1, b, c, d, e;
int
main ()
{
    for (;; d++)
    {
        e = 0;
        for (; e >= 0; e--)
            if (a);
            else
                c = b <= 0;
        c = b <= 0;
        break;
    }
    printf ("%d\n", c);
    return 0;
}
