int printf (const char *, ...);
int a;
int *b, *c = &a;
int
fn1 (int p1, int p2)
{
    return p1 && p2 && 0 > -p2 || p1 && p2 < 0 < -p2 ? p1 : p1 + p2;
}

int
main ()
{
    int **d = &b;
    if (fn1 (&b != d, 8L))
        *c = 1;
    printf ("%d\n", a);
    return 0;
}
