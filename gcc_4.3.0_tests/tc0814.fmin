int printf (const char *, ...);
int ****a;
int b;
int *c = &b;
int ***d;
int
fn1 (int p1, int p2)
{
    return p1 ? 0 : p1 - p2;
}

int
main ()
{
    if (fn1 ((a = &d) != &d, 10L))
        *c = 1;
    printf ("%d\n", b);
    return 0;
}
