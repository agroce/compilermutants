int printf (const char *, ...);
char a;
int b;
char *d = &a;
void fn1 (int *);
void
fn2 (p1)
{
    int *c = &b;
    *c = p1;
    fn1 (&b);
}

void
fn1 (int *p1)
{
    *d = 6 ^ (0, *p1);
}

int
main ()
{
    int *e = &b, *f = &b;
    fn2 (e != f);
    printf ("%d\n", a);
    return 0;
}
