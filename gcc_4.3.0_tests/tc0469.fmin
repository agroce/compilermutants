int printf (const char *, ...);
int a, d = 1, f;
struct U0
{
    int f0;
}
e =
{
    1
};

int *b;
static int **c[] = { &b };

short g;
int
fn1 (p1)
{
    return a == 0 ? p1 : 0 % 0;
}

int
fn2 (int p1, int p2)
{
    return p2 || 0 / 0 ? p1 : p1 * p2;
}

int
main ()
{
    struct U0 h = e;
    int **i = &b;
    g = fn2 (46495L, h.f0);
    f = 1 ? g : 0;
    if (fn1 ((c[0] != i) < f))
        d = 0;
    printf ("%d\n", d);
    return 0;
}
