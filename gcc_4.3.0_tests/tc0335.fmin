int printf (const char *, ...);
int a, b, d;
int *c;
void
fn1 (p1)
{
    b = 1;
    for (; b >= 0; b--)
    {
        for (; a <= 0; a++)
            if (b)
                break;
        if (p1)
            break;
    }
}

int
main ()
{
    int **e = &c;
    d = 1 << 1;
    fn1 ((&c == e) < d);
    printf ("%d\n", a);
    return 0;
}
