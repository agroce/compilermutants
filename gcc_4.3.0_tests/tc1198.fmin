int printf (const char *, ...);
int a, d, f;
int *b = &a, *c = &a, *e = &d;
int
fn1 (char p1, char p2)
{
    return p1 > 0 && p1 > 127 / p2 <= 0;
}

int
main ()
{
    *b = 1;
    int g = 1914L;
    *e = fn1 (0 >= f && *c, g);
    printf ("%d\n", d);
    return 0;
}
