int printf (const char *, ...);
int a, b, c, d, e, f;
char
fn1 (char p1)
{
    return (p1 ^ 1) & (p1 ^ 7) - 1 ? p1 : p1 - 1;
}

void
fn2 (p1)
{
    for (; c <= 0; c = c + 1)
        if (p1)
        {
            f = a == 0 ? 0 : 0 % 0;
            e = f + 1;
            d = fn1 (e > p1);
        }
}

int
main ()
{
    for (; b;)
        for (;;);
    fn2 (1);
    printf ("%d\n", d);
    return 0;
}
